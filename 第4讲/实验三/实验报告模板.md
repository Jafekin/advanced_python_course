# Python 进阶 

## 实验三报告：NLP 基础与 Prompt 工程

|   **姓名**   |        （填写）         |
| :----------: | :---------------------: |
|   **学号**   |        （填写）         |
|   **班级**   |        （填写）         |
| **提交日期** |        （填写）         |
| **实验平台** | CloudStudio + CodeBuddy |

---

## 一、实验概述

请对照实验指导书，勾选完成情况：

- [ ] 任务一：BPE 分词算法实现与 tiktoken 对比（必做）
- [ ] 任务二：Prompt Engineering 三种策略对比实验（必做）
- [ ] 任务三：大模型 API 应用开发（情感分类 + 信息抽取 + 文本摘要）（**选做**，完成任务一、二即视为完成必做部分）

---

## 二、任务一：BPE 分词算法实现（40分）

### 2.1 算法实现说明

简要描述你的 `train_bpe` 和 `bpe_encode` 函数的实现思路（100字以内）：

（在此填写）

### 2.2 训练过程输出

将以下语料的 BPE 训练日志粘贴于此：

```
语料：{"study": 3, "student": 5, "studio": 2, "state": 4, "stable": 2, "stem": 3}
目标词表大小：12
```

**训练日志**（运行代码后粘贴输出）：

```text
（粘贴每步合并日志）
```

**最终词表**：

```text
（粘贴词表内容）
```

### 2.3 测试词分词结果

| 测试词       | 你的 BPE 分词结果 | tiktoken 分词结果 | 是否一致 |
| --------- | ----------- | ------------- | ---- |
| students  |             |               |      |
| studies   |             |               |      |
| statement |             |               |      |
| unstable  |             |               |      |

### 2.4 结果分析

你的 BPE 分词结果与 tiktoken 的结果是否一致？请分析原因（50字以上）：

（在此填写）

---

## 三、任务二：Prompt Engineering 对比实验（60分）

### 3.1 三组 Prompt 设计

**A组（Zero-shot）**：

```
（粘贴你设计的 Zero-shot Prompt）
```

**B组（Few-shot）**：

```
（粘贴你设计的 Few-shot Prompt，包含3个示例）
```

**C组（CoT）**：

```
（粘贴你设计的 CoT Prompt）
```

### 3.2 实验结果对比表

**测试数据**：reviews.json 中 id 1-10 的10条评论

| 指标              | A组（Zero-shot） | B组（Few-shot） | C组（CoT） |
| --------------- | ------------- | ------------ | ------- |
| 准确率（/10）        |               |              |         |
| JSON 解析成功率（/10） |               |              |         |
| 平均 Token 消耗     |               |              |         |
| 平均响应时间（秒）       |               |              |         |

### 3.3 错误案例分析

列举 1-2 个预测错误的案例，分析错误原因：

| 案例 id | 原文  | 真实标签 | 模型预测 | 错误原因分析 |
| ----- | --- | ---- | ---- | ------ |
|       |     |      |      |        |
|       |     |      |      |        |

### 3.4 结论分析（100字以上）

（在此填写：哪种策略在本任务上最优？为什么？不同策略的 Token 消耗差异说明了什么？）

---

## 四、任务三：API 应用开发（35分，选做）

### 4.1 函数设计说明

简要说明三个函数使用的 Prompt 策略选择理由（每个函数30字以内）：

- `classify_sentiment`：（填写）
- `extract_info`：（填写）
- `summarize`：（填写）

### 4.2 情感分类结果截图（20条）


### 4.3 信息抽取结果（奇数 id，共10条）

（粘贴 extract_info 对10条数据的输出 JSON，每条一个代码块）

```json
// id=1
（粘贴）
```

```json
// id=3
（粘贴）
```

（以此类推...）

### 4.4 课程评价综合摘要

（粘贴 summarize 对全部20条评论生成的摘要）

```text
（粘贴摘要内容）
```

### 4.5 API 成本统计

| 函数                 | 调用次数 | 总 Token 消耗 | 估算费用（¥1/百万Token） |
| ------------------ | ---- | ---------- | ---------------- |
| classify_sentiment | 20   |            |                  |
| extract_info       | 10   |            |                  |
| summarize          | 1    |            |                  |
| **合计**             | 31   |            |                  |

---

## 五、思考题（三选一）

请选择以下一道思考题作答（150字以上）：

**选择题号**：□ a  □ b  □ c

**题目**：

> a. Temperature=0 和 Temperature=1.5 分别适合什么场景？如果你要做一个代码生成助手，应该怎么设置采样参数组合？
> 
> b. 思维链（CoT）为什么能提升推理准确率？它有什么局限性？在什么场景下反而会降低性能？
> 
> c. 大模型的"幻觉"问题在实际应用中如何检测？你认为 RAG 和 CoT 自我验证哪种方式更适合"事实性问答"场景？

**回答**：

（在此填写，不少于150字）

---

## 六、实验总结

### 6.1 遇到的问题与解决方法

（在此填写）
### 6.2 本次实验最大收获（50字以上）

（在此填写）

### 6.3 对课程的建议（选填）

（在此填写）
