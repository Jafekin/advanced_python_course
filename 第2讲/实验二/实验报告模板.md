# Python 进阶
## 实验二报告：机器学习基础与 AI 协作建模

|   **姓名**   |        （填写）         |
| :----------: | :---------------------: |
|   **学号**   |        （填写）         |
|   **班级**   |        （填写）         |
| **提交日期** |        （填写）         |
| **实验平台** | CloudStudio + CodeBuddy |

## 一、实验目的

1. 掌握线性回归、SVM 分类、K-means 聚类、PCA 降维四类 ML 算法
2. 学会编写 speckit.md 规约文件（constitution + context + 任务列表）
3. 用 @speckit.md 引用驱动 CodeBuddy 按规约生成代码
4. 体验"规约先行 → AI 执行 → 人工验证"的协作建模工作流

## 二、实验环境

| | |
|---|---|
| **开发平台** | CloudStudio（cloudstudio.net） |
| **AI 助手** | CodeBuddy（Craft 模式） |
| **Python** | 3.10+ |
| **依赖库** | pandas, numpy, matplotlib, scikit-learn, openpyxl |

## 三、实验内容与记录

### 实验1：线性回归 + 简单规约（25分）

数据集：sklearn `load_diabetes()`（糖尿病患者数据，442样本，用 BMI 预测疾病进展）

speckit_exp1.md 内容：

```markdown
（粘贴你编写的 speckit_exp1.md 完整内容）
```

@speckit_exp1.md 引用截图：

> （粘贴你使用 @speckit_exp1.md 引用执行任务的对话截图）

运行结果截图：

> （粘贴：回归方程输出、RMSE 输出、散点图）

代码逻辑说明（用自己的话，3-5句）：

> 例：用 load_diabetes() 加载数据，提取第3列 BMI 特征并 reshape(-1,1) 转为列向量，fit 训练 LinearRegression 得到回归系数和截距，用 predict 生成预测值后计算 RMSE，最后画散点图叠加预测线。

关键结果：

| 指标 | 值 |
|------|-----|
| 回归方程 | y = ____x + ____ |
| RMSE | |

---

### 实验2：SVM 分类 + 规约驱动（30分）

数据集：sklearn `load_wine()`（红酒化学成分数据，178样本 × 13特征 → 3种品种）

speckit_exp2.md 内容：

```markdown
（粘贴你编写的 speckit_exp2.md 完整内容）
```

@speckit_exp2.md 引用截图：

> （粘贴你使用 @speckit_exp2.md 引用执行任务的对话截图）

运行结果截图：

> （粘贴：F1-score 输出、决策边界图）

代码逻辑说明（用自己的话，3-5句）：

> 例：用 load_wine() 加载红酒数据后划分训练测试集，用 rbf 核 SVM 训练并 predict，计算 F1-score 评估分类效果，取酒精度和颜色强度两个特征用网格铺满特征空间让 SVM 逐点预测画出决策边界。

关键结果：

| 指标 | 值 |
|------|-----|
| F1-score | |

---

### 实验3：乳腺癌数据聚类与降维验证（45分）

数据集：sklearn `load_breast_cancer()`（乳腺癌诊断数据，569样本 × 30特征）

speckit_exp3.md 内容：

```markdown
（粘贴你编写的 speckit_exp3.md 完整内容）
```

各任务 @speckit_exp3.md 引用截图：

> 任务1截图：（粘贴）
>
> 任务2截图：（粘贴）
>
> 任务3截图：（粘贴）
>
> 任务4截图：（粘贴）

运行结果截图：

> （粘贴：肘部曲线图、各簇样本数输出、PCA 2D 散点图）

代码逻辑说明（用自己的话，4-6句）：

> 例：用 load_breast_cancer() 加载 569 样本 ×30 特征的数据，用肘部法则跑 K=2-9 的 KMeans 记录 inertia 画曲线找拐点，选定 K 后用 k-means++ 训练并打印各簇样本数，最后 StandardScaler 标准化后 PCA 降至 2 维按聚类标签上色验证分群质量。

关键结果：

| 指标 | 值 |
|------|-----|
| 选定 K 值 | |
| 选择依据 | |
| 各簇样本数 | |
| 主成分1 解释方差比 | |
| 主成分2 解释方差比 | |
| 累计解释方差 | |

---

## 四、思考题

### 思考题 1

请用简明扼要的语句概括分类与聚类的不同，并各举两个例子说明应用场景。

回答：

> （200字以上）

### 思考题 2

对比两段式 Prompt 和 Speckit 规约驱动两种方式，各适合什么场景？在你完成实验3的过程中，Speckit 带来了什么具体好处？

回答：

> （200字以上）

---

## 五、实验总结

（3-5句话：对 Speckit 规约驱动的真实感受、遇到的困难、与两段式 Prompt 的体验对比）

>

---

