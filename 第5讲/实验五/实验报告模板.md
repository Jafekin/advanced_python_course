# 实验五 实验报告

## 基本信息

| 项目 | 内容 |
|------|------|
| 课程名称 | Python 进阶（计算机学院） |
| 实验名称 | NLP 进阶与模型微调 |
| 姓名 | |
| 学号 | |
| 班级 | |
| 完成日期 | |

---

## 任务1：Transformer 自注意力机制实现（35分）

### 1.1 实现思路

请简要描述你对自注意力机制计算流程的理解（Q/K/V 的作用、缩放的目的、Softmax 的意义）：

> （在此填写）

### 1.2 核心代码

请粘贴你补全的 `softmax` 和 `scaled_dot_product_attention` 函数代码：

```python
# 在此粘贴代码
```

### 1.3 运行结果

请粘贴程序运行输出（包含注意力权重矩阵和注意力输出）：

```text
# 在此粘贴输出
```

### 1.4 结果分析

1. 观察注意力权重矩阵，每行的和是否为 1？为什么必须如此？

> （在此填写）

2. 如果去掉缩放因子 $\sqrt{d_k}$，注意力权重会发生什么变化？（可尝试去掉后观察输出）

> （在此填写）

---

## 任务2：BERT 情感分类微调（65分）

### 2.1 实验环境

| 项目 | 配置 |
|------|------|
| 运行平台 | CloudStudio / 本地（请选择） |
| Python 版本 | |
| PyTorch 版本 | |
| Transformers 版本 | |
| 设备 | CPU / GPU（请选择） |

### 2.2 核心代码

请分别粘贴你补全的 4 个部分代码：

**TODO 1: load_data**
```python
# 在此粘贴
```

**TODO 2: CommentDataset.__getitem__**
```python
# 在此粘贴
```

**TODO 3: train_model 训练循环**
```python
# 在此粘贴
```

**TODO 4: main 函数中的 tokenizer 和 model 初始化**
```python
# 在此粘贴
```

### 2.3 训练过程

请记录每轮的训练 Loss：

| Epoch | Train Loss |
|-------|-----------|
| 1 | |
| 2 | |
| 3 | |

Loss 曲线是否正常下降？如果出现异常请描述：

> （在此填写）

### 2.4 预测结果

请粘贴测试样例的预测输出：

```text
# 在此粘贴
```

预测准确率统计（6 条测试样例）：

| 正确预测数 | 总数 | 准确率 |
|-----------|------|--------|
| | 6 | |

### 2.5 结果分析

1. 如果有预测错误的样例，分析可能的原因：

> （在此填写）

2. 你认为可以通过哪些方式提升模型表现？（至少列举 2 种）

> （在此填写）

---

## 任务3：LoRA 轻量微调对比（选做，+30分）

> 如未完成此任务，可跳过此部分。

### 3.1 LoRA 配置

请填写你使用的 LoRA 超参数：

| 参数 | 值 |
|------|---|
| r（秩） | |
| lora_alpha | |
| lora_dropout | |
| target_modules | |

### 3.2 参数量对比

```text
# 粘贴 model.print_trainable_parameters() 的输出
```

### 3.3 对比结果

| 对比项 | 全参数微调 | LoRA 微调 |
|--------|-----------|----------|
| 可训练参数量 | | |
| 训练时间（3 epochs） | | |
| 最终 Loss | | |
| 测试样例准确率 | | |

### 3.4 分析

1. LoRA 微调相比全参数微调的优势和劣势分别是什么？

> （在此填写）

2. 为什么 LoRA 只对 query 和 value 矩阵添加适配器，而不是所有层？

> （在此填写）

---

## 思考题（三选一）

**我选择的题目编号：____**

> （在此填写你的回答，要求条理清晰、有理有据，200-400 字）

---

## 实验总结

### 收获与体会

> 请总结本次实验的主要收获（学到了什么新知识/技能？遇到了什么困难？如何解决的？）

### 改进建议

> 对本实验的建议（难度、时长、内容等方面）
