# Python 进阶
## 实验五报告：大模型部署与检索生成增强（RAG）

|   **姓名**   |        （填写）         |
| :----------: | :---------------------: |
|   **学号**   |        （填写）         |
|   **班级**   |        （填写）         |
| **提交日期** |        （填写）         |
| **实验平台** | CloudStudio + CodeBuddy |

## 一、实验目的

1. 掌握大模型本地部署：用 Ollama 起 OpenAI 兼容 API 服务（或云端 API 替代）
2. 掌握 RAG 完整流水线：文档加载 → 分块 → 向量化 → ChromaDB → 检索 → 生成
3. **掌握英文长文档的分块策略**（英文递归字符分块，分隔符 `. \n → \n\n → \n → . → ; → ,`）
4. **掌握跨语言检索**：用中文查询检索英文文档（嵌入模型选型与中英文对齐）
5. 掌握高级检索策略：MQE 多查询扩展 / HyDE 假设文档嵌入 / Reranker 重排序
6. 学会编写 `rag_speckit.md` 规约文件，用 Spec-driven 方式管理 RAG 项目

## 二、实验环境

| | |
|---|---|
| **开发平台** | CloudStudio（cloudstudio.net） |
| **AI 助手** | CodeBuddy（Craft 模式） |
| **Python** | 3.10+ |
| **LLM 服务** | ☐ 本地 Ollama（模型：______）  ☐ 云端 API（服务：______） |
| **依赖库** | sentence-transformers, chromadb, openai, markitdown, pypdf |
| **实验数据** | `综述论文.pdf`（ACM TOSEM 2025，"LLM for Mobile: An Initial Roadmap"，29 页英文综述） |

## 三、实验内容与记录

### 任务1：英文文档加载与分块（25 分）

数据集：`综述论文.pdf`（ACM TOSEM 2025 综述，29 页英文，3.1MB）

**rag_speckit.md 的 constitution 和 context 部分**：

```markdown
（粘贴你编写的 rag_speckit.md 的 constitution + context 部分）
```

**英文分块函数实现**：

```python
（粘贴你的 chunk_recursive_en 函数代码，注意分隔符优先级：". \n" → "\n\n" → "\n" → ". " → "; " → ", " → " "）
```

**三种 chunk_size 对比**：

| chunk_size | 块数 | 平均块长（字符） | 最长块 | 最短块 | 是否有跨段切断 |
|------------|------|-----------------|--------|--------|----------------|
| 400 | | | | | |
| 600 | | | | | |
| 1000 | | | | | |

**分析**：chunk_size 选择 ______，因为 ______。

**英文分块与中文分块的关键差异**：

| 维度 | 中文分块（notebook） | 英文分块（本实验） |
|------|---------------------|-------------------|
| 句号 | `。` | `. `（句点+空格） |
| 分隔符优先级 | `\n\n → \n → 。 → ； → ， → 空格` | `. \n → \n\n → \n → . → ; → , → 空格` |
| 词边界 | 无空格 | 空格分词 |
| 清洗难点 | emoji、全角符号 | 页眉页脚（ACM Transactions...） |

@rag_speckit.md 引用截图：

> （粘贴你使用 @rag_speckit.md 引用执行任务1 的对话截图）

---

### 任务2：向量化与向量库存储（30 分）

**嵌入模型**：______（如 all-MiniLM-L6-v2，384 维）

**ChromaDB 存储验证**：

```python
（粘贴创建 collection 和添加文档的代码，metadata 至少含 chunk_idx / source / section）
```

**collection 统计**：

| 指标 | 值 |
|------|-----|
| collection 名称 | |
| 记录数 | |
| 向量维度 | |
| metadata 字段 | chunk_idx, source, section, ... |

**metadata 设计说明**（section 字段如何提取）：

> （说明如何用章节标题 `1 Introduction`、`2 Preparing Dataset...` 等作为分块边界，给每个 chunk 打 section 标签）

**测试检索结果**（查询 "on-device LLM security threats" top-3）：

| 排名 | chunk_idx | section | score | 内容前 80 字 |
|------|-----------|---------|-------|--------------|
| 1 | | | | |
| 2 | | | | |
| 3 | | | | |

---

### 任务3：检索与生成（25 分）

**Prompt 模板**（英文系统指令 + 同语言回答 + 引用标注）：

```python
（粘贴你的 PROMPT_TEMPLATE，注意：英文系统指令、要求用与问题相同的语言回答、带 [1][2] 引用标注）
```

**5 个预设问题的回答**：

**Q1（中文）：移动端部署大模型有哪些主要挑战？**

回答：
> （粘贴 LLM 回答）

引用来源：chunk ______（section=______，score=______）

---

**Q2（中文）：论文讨论了哪些微调数据集准备方法？**

回答：
> （粘贴 LLM 回答）

引用来源：chunk ______（section=______，score=______）

---

**Q3（英文）：What are the security threats to on-device LLMs?**

回答：
> （粘贴 LLM 回答）

引用来源：chunk ______（section=______，score=______）

---

**Q4（中文）：LLM 如何用于移动应用的运行时监控？**

回答：
> （粘贴 LLM 回答）

引用来源：chunk ______（section=______，score=______）

---

**Q5（英文）：Which dataset preparation techniques are discussed for fine-tuning?**

回答：
> （粘贴 LLM 回答）

引用来源：chunk ______（section=______，score=______）

---

### 任务4：高级检索对比（20 分）

**选择的高级检索策略**：☐ MQE 多查询扩展  ☐ HyDE 假设文档嵌入  ☐ Reranker 重排序

**实现代码**：

```python
（粘贴你的高级检索实现代码，注意：MQE 扩展查询要与原查询同语言；HyDE 假设答案要用英文生成以与英文文档对齐）
```

**Recall@5 对比表**：

| 问题 | 相关 chunk_id（人工标注） | 基础检索 Recall@5 | 高级检索 Recall@5 | 提升 |
|------|--------------------------|-------------------|-------------------|------|
| Q1 | | | | |
| Q2 | | | | |
| Q3 | | | | |
| Q4 | | | | |
| Q5 | | | | |
| **平均** | — | | | |

**分析**：高级检索策略的效果评估 ______，因为 ______。在跨语言场景下（中文查询 vs 英文文档）效果差异是否比同语言场景更明显？为什么？

---

## 四、实验总结

### 4.1 遇到的问题和解决方法

| 问题 | 解决方法 |
|------|---------|
| （如：sentence-transformers 下载慢） | （设置 HF_ENDPOINT 镜像） |
| （如：英文 PDF 页眉页脚干扰分块） | （过滤 "ACM Transactions"、"LLM for Mobile" 等重复行） |
| （如：中文查询检索英文文档召回率低） | （改用 bge-m3 多语言模型 / 或用 MQE 扩展成中英文混合查询） |
| | |

### 4.2 RAG 流水线各环节对效果的影响

| 环节 | 调参经验 | 对效果的影响 |
|------|---------|-------------|
| 文档加载 | markitdown 转换质量 + 页眉页脚清洗 | ______ |
| 文本分块 | chunk_size / overlap / 英文分隔符 | ______ |
| 向量化 | 嵌入模型选型（英文 vs 多语言） | ______ |
| 向量库 | metadata 含 section 字段 | ______ |
| 检索 | top_k / score_threshold | ______ |
| 生成 | Prompt 模板（同语言回答）/ temperature | ______ |

### 4.3 跨语言检索的难点与对策

- 中文查询 vs 英文文档的语义对齐问题：______
- 使用的对策（选一）：☐ 换 bge-m3 多语言模型  ☐ MQE 扩展成中英文混合查询  ☐ HyDE 生成英文假设答案
- 效果评估：______

### 4.4 高级检索策略效果评估

- 实现的策略：______
- Recall@5 提升幅度：______
- 适用场景：______
- 局限性：______

### 4.5 实验心得

（300-500 字，谈谈对 RAG 的理解、英文文档与中文文档 RAG 的差异、跨语言检索的难点、Vibe Coding 工具的价值、与第4讲微调的对比等）

---

## 五、附录

### 5.1 完整代码

```python
（粘贴完整 RAG 流水线代码，含任务1-4 的所有函数）
```

### 5.2 rag_speckit.md 完整内容

```markdown
（粘贴完整 rag_speckit.md）
```
