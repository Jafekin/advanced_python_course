# 实验七：RAG 进阶与 Agent 记忆系统

## 实验信息

| 项目 | 内容 |
|------|------|
| 课程 | Python 进阶 |
| 实验名称 | RAG 进阶与 Agent 记忆系统 |
| 对应讲次 | 第7讲：RAG 进阶与 Agent 记忆系统 |
| 实验学时 | 2 学时（课堂完成为主，课外补完） |
| 实验类型 | 综合性实验 |
| 总分 | 100 分 |

## 实验目的

1. 实现 MQE 多查询扩展，对比单查询 vs 多查询的检索效果
2. 实现 Reranker 两阶段检索，量化精度提升
3. 构建评估数据集，计算 Recall@k 和 MRR 指标
4. 实现简单 Agent 记忆系统（语义记忆 + 情景记忆）

## 实验环境

- 平台：CloudStudio + CodeBuddy
- Python 3.10+ + openai + sentence-transformers + chromadb + rank-bm25 + jieba
- 基于第6讲实验已建好的 ChromaDB 向量库

## 前置准备

```bash
pip install rank-bm25 jieba
```

确保第6讲实验的 `./chroma_db` 目录存在且包含论文数据。

---

## 任务1：MQE 多查询扩展（25分）

### 目标

实现多查询扩展检索，对比单查询 vs MQE 的检索效果。

### 具体要求

1. **实现查询改写函数**：

```python
def generate_multi_queries(question, n=3):
    """用 LLM 生成 N 个等价查询"""
    prompt = f"""请将以下问题改写成{n}个不同表述的查询，
每个查询用不同的关键词，但语义相同。每行一个，不要编号。

原始问题：{question}"""
    # 调用 LLM 生成改写
    ...
    return [question] + queries[:n]
```

2. **实现 MQE 检索函数**：

```python
def mqe_retrieve(question, top_k=5, n_queries=3):
    """多查询扩展检索：改写 → 并行检索 → 合并去重"""
    ...
```

3. **效果对比**：选择 3 个测试问题，分别用单查询和 MQE 检索，对比 top-5 结果

4. **输出**：对比表格（单查询命中 vs MQE 命中）

### 评分标准

| 评分项 | 分值 | 标准 |
|--------|:---:|------|
| 查询改写函数正确 | 8分 | 能生成 3 个语义等价但表述不同的查询 |
| MQE 检索函数实现 | 10分 | 并行检索 + 合并去重逻辑正确 |
| 效果对比表格 | 7分 | 至少 3 个问题的对比，能看出 MQE 优势 |

---

## 任务2：Reranker 重排序（25分）

### 目标

实现两阶段检索（Bi-Encoder 粗排 + Cross-Encoder 精排），量化精度提升。

### 具体要求

1. **加载 Reranker 模型**：

```python
from sentence_transformers import CrossEncoder

reranker = CrossEncoder('BAAI/bge-reranker-base')
```

2. **实现两阶段检索**：

```python
def rerank_retrieve(question, top_k=5, rerank_top=20):
    """粗排 top-20 → Cross-Encoder 精排 → 取 top-5"""
    # 阶段1：向量检索 top-20
    # 阶段2：Cross-Encoder 打分
    # 按分数重排，返回 top-5
    ...
```

3. **效果对比**：选择 3 个测试问题，对比"纯向量 top-5" vs "Reranker top-5"

4. **输出**：每个问题展示重排前后的排序变化

### 评分标准

| 评分项 | 分值 | 标准 |
|--------|:---:|------|
| CrossEncoder 加载成功 | 5分 | 模型加载无报错 |
| 两阶段检索实现 | 12分 | 粗排+精排逻辑正确 |
| 效果对比展示 | 8分 | 能看出重排后更相关的结果排到前面 |

---

## 任务3：构建评估数据集 + 计算指标（25分）

### 目标

构建至少 10 个问答对的评估数据集，计算 Recall@5 和 MRR。

### 具体要求

1. **构建评估数据集**（至少 10 个问答对）：

```python
test_set = [
    {
        "question": "...",
        "answer_chunk_id": "chunk_X",  # 人工标注正确 chunk
    },
    # ... 至少 10 个
]
```

构建方法：浏览 chunk 内容 → 针对每个 chunk 设计一个问题 → 记录对应 chunk_id

2. **实现评估函数**：

```python
def evaluate_retrieval(test_set, retrieve_fn, top_k=5):
    """计算 Recall@k 和 MRR"""
    ...
    return {"Recall@k": ..., "MRR": ...}
```

3. **对比三种检索方法**：

| 方法 | Recall@5 | MRR |
|---|---|---|
| 基础向量检索 | | |
| MQE（任务1） | | |
| Reranker（任务2） | | |

### 评分标准

| 评分项 | 分值 | 标准 |
|--------|:---:|------|
| 评估数据集质量 | 10分 | 至少 10 个问答对，问题有难度梯度 |
| 评估函数正确 | 8分 | Recall@k 和 MRR 计算逻辑正确 |
| 三方法对比表格 | 7分 | 数据合理，能体现高级策略的优势 |

---

## 任务4：简单记忆系统（25分）

### 目标

实现一个简单的 Agent 记忆系统，支持"记住用户信息"并在后续对话中使用。

### 具体要求

1. **实现语义记忆**（基于 ChromaDB）：

```python
class SemanticMemory:
    def store(self, fact, metadata=None): ...
    def recall(self, query, top_k=3): ...
```

2. **实现情景记忆**（基于 JSON）：

```python
class EpisodicMemory:
    def save_episode(self, event, importance=0.5): ...
    def recall_recent(self, n=5): ...
```

3. **实现带记忆的对话 Agent**：

```python
class MemoryAgent:
    def chat(self, user_message): ...
```

4. **对话演示**（至少 3 轮，证明记忆生效）：
   - 第1轮：告诉 Agent 你的信息（如"我是计算机学院的学生，正在学 Python"）
   - 第2-3轮：问一些与之前信息相关的问题，验证 Agent 能否回忆

### 评分标准

| 评分项 | 分值 | 标准 |
|--------|:---:|------|
| 语义记忆实现 | 8分 | store + recall 逻辑正确 |
| 情景记忆实现 | 7分 | save + recall_recent 逻辑正确 |
| 对话演示 | 10分 | 至少 3 轮对话，第3轮能回忆第1轮的信息 |

---

## 提交要求

1. 可运行的 Jupyter Notebook（含全部 4 个任务的代码和输出）
2. 实验报告（使用提供的模板）
3. 任务3 的评估对比表格截图
4. 任务4 的多轮对话截图

## 注意事项

- 任务1-3 基于第6讲的 ChromaDB 数据，确保 `./chroma_db` 可用
- CrossEncoder 模型较大（~400MB），首次加载需等待下载
- 任务4 的记忆系统是简化版，不需要实现时间衰减（选做加分）
- 如果 LLM 服务不可用，MQE 的改写可以手动编写替代
